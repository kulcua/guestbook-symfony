version: '3'

services:
    database:
        image: postgres:13-alpine
        environment:
            POSTGRES_USER: kulcua
            POSTGRES_PASSWORD: kul123
            POSTGRES_DB: book
        ports: [5432]

    redis:
        image: redis:5-alpine
        ports: [6379]